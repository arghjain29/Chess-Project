<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>

    <title>Document</title>
    <style>
      .chessboard {
        display: grid;
        grid-template-columns: repeat(8, minmax(0, 1fr));
        grid-template-rows: repeat(8, minmax(0, 1fr));
        width: 400px;
        height: 400px;
        transform: rotate(0deg);
      }

      .piece {
        font-family: "Arial Unicode MS", "Noto Sans Symbols", sans-serif;
        font-size: 36px;
        cursor: grab !important;
        user-select: none !important;
      }

      .piece.white {
        color: white;
        filter: drop-shadow(0 0 2px rgba(0, 0, 0, 1));
      }

      .piece.black {
        color: #000000;
        filter: drop-shadow(0 0 2px rgb(223, 218, 218));
      }

      .flipped {
        transform: rotate(180deg);
      }

      .square {
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .square.light {
        background-color: #f0d9b5;
      }

      .square.dark {
        background-color: #b58863;
      }

      .piece.draggable {
        cursor: grab;
      }

      .dragging {
        opacity: 0.5; /* Reduce opacity for a better visual effect */
        cursor: grabbing;
        background: none; /* Ensure no square background is visible */
        box-shadow: none; /* Remove any shadow if applied */
      }

      /* Rotate pieces for black player */
      .flipped .piece {
        transform: rotate(180deg);
      }
    </style>
  </head>
  <body>
    <div class="h-screen flex flex-col items-center justify-center bg-zinc-900">
      <!-- Role or Status Message -->
      <div class="mb-20 flex items-center justify-center">
        <h3 class="text-3xl font-semibold text-white">Connecting...</h3>
      </div>
    
      <!-- Chessboard (Hidden Until Ready) -->
      <div class="chessboard w-96 h-96 grid grid-cols-8 grid-rows-8 hidden" id="chessboard">
        <!-- The chessboard will be rendered dynamically -->
      </div>
    </div>
    
    

    <script
      src="https://cdn.socket.io/4.8.1/socket.io.min.js"
      integrity="sha384-mkQ3/7FUtcGyoppY6bz/PORYoGqOl7/aSUMn2ymDOJcapfS6PHqxhRTMh1RR0Q6+"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"
      integrity="sha512-xRllwz2gdZciIB+AkEbeq+gVhX8VB8XsfqeFbUh+SzHlN96dEduwtTuVuc2u9EROlmW9+yhRlxjif66ORpsgVA=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    ></script>
    <script src="/javascripts/chessgame.js"></script>
  </body>
</html>
